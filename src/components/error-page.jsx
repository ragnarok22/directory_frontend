import { React } from "react";
import { outPut } from "../security";

export const ErrorComponent = () => (
  <div className="flex justify-center items-center w-full h-full">
    <p>{outPut}</p>
  </div>
);
